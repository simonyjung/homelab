apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-data-pvc
spec:
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteMany
  securityContext:
    fsGroup: 33