apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-data-pvc
spec:
  storageClassName: synology-iscsi-retain
  resources:
    requests:
      storage: 500Mi
  accessModes:
    - ReadWriteOnce
  securityContext:
    fsGroup: 33